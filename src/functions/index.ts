import {
  formatDate,
  padTo2Digits,
  parseDateString,
  returnMaxDate,
  returnMinDate,
} from './returnDate';

export const functions = {
  formatDate,
  padTo2Digits,
  parseDateString,
  returnMaxDate,
  returnMinDate,
};
