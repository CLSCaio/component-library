export interface SizesProps {
  xSmall: `${number}rem`;
  small: `${number}rem`;
  default: `${number}rem`;
  medium: `${number}rem`;
  large: `${number}rem`;
  xLarge: `${number}rem`;
  huge: `${number}rem`;
}
